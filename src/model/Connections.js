/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 1.25.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Connections model module.
 * @module model/Connections
 * @version 1.25.0
 */
class Connections {
    /**
     * Constructs a new <code>Connections</code>.
     * Secure connections from Spartera to your databases and data warehouses
     * @alias module:model/Connections
     * @param engineId {Number} References storage_engines.engine_id — Fact table of all the different storage engines we support. See GET /storage_engines for valid values. Required.
     * @param companyId {String} References companies.company_id — A Spartera seller or buyer company account. See GET /companies for valid values. Required.
     */
    constructor(engineId, companyId) { 
        
        Connections.initialize(this, engineId, companyId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, engineId, companyId) { 
        obj['engine_id'] = engineId;
        obj['company_id'] = companyId;
    }

    /**
     * Constructs a <code>Connections</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Connections} obj Optional instance to populate.
     * @return {module:model/Connections} The populated <code>Connections</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Connections();

            if (data.hasOwnProperty('date_created')) {
                obj['date_created'] = ApiClient.convertToType(data['date_created'], 'Date');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('connection_id')) {
                obj['connection_id'] = ApiClient.convertToType(data['connection_id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('engine_id')) {
                obj['engine_id'] = ApiClient.convertToType(data['engine_id'], 'Number');
            }
            if (data.hasOwnProperty('company_id')) {
                obj['company_id'] = ApiClient.convertToType(data['company_id'], 'String');
            }
            if (data.hasOwnProperty('credential_type')) {
                obj['credential_type'] = ApiClient.convertToType(data['credential_type'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('provider_domain')) {
                obj['provider_domain'] = ApiClient.convertToType(data['provider_domain'], 'String');
            }
            if (data.hasOwnProperty('verified_usage_ability')) {
                obj['verified_usage_ability'] = ApiClient.convertToType(data['verified_usage_ability'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Connections</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Connections</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Connections.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['connection_id'] && !(typeof data['connection_id'] === 'string' || data['connection_id'] instanceof String)) {
            throw new Error("Expected the field `connection_id` to be a primitive type in the JSON string but got " + data['connection_id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['company_id'] && !(typeof data['company_id'] === 'string' || data['company_id'] instanceof String)) {
            throw new Error("Expected the field `company_id` to be a primitive type in the JSON string but got " + data['company_id']);
        }
        // ensure the json data is a string
        if (data['credential_type'] && !(typeof data['credential_type'] === 'string' || data['credential_type'] instanceof String)) {
            throw new Error("Expected the field `credential_type` to be a primitive type in the JSON string but got " + data['credential_type']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['provider_domain'] && !(typeof data['provider_domain'] === 'string' || data['provider_domain'] instanceof String)) {
            throw new Error("Expected the field `provider_domain` to be a primitive type in the JSON string but got " + data['provider_domain']);
        }

        return true;
    }


}

Connections.RequiredProperties = ["engine_id", "company_id"];

/**
 * Optional.
 * @member {Date} date_created
 */
Connections.prototype['date_created'] = undefined;

/**
 * Optional.
 * @member {Date} last_updated
 */
Connections.prototype['last_updated'] = undefined;

/**
 * Unique identifier.
 * @member {String} connection_id
 */
Connections.prototype['connection_id'] = undefined;

/**
 * References users.user_id — An individual user account within a company. See GET /users for valid values. Optional.
 * @member {String} user_id
 */
Connections.prototype['user_id'] = undefined;

/**
 * References storage_engines.engine_id — Fact table of all the different storage engines we support. See GET /storage_engines for valid values. Required.
 * @member {Number} engine_id
 */
Connections.prototype['engine_id'] = undefined;

/**
 * References companies.company_id — A Spartera seller or buyer company account. See GET /companies for valid values. Required.
 * @member {String} company_id
 */
Connections.prototype['company_id'] = undefined;

/**
 * Optional. One of: SERVICE_ACCOUNT, USERNAME_PASSWORD, API_KEY, SERVICE_IDENTITY, ACCESS_KEY, … (8 total).
 * @member {module:model/Connections.CredentialTypeEnum} credential_type
 */
Connections.prototype['credential_type'] = undefined;

/**
 * Optional.
 * @member {String} name
 */
Connections.prototype['name'] = undefined;

/**
 * Optional.
 * @member {String} description
 */
Connections.prototype['description'] = undefined;

/**
 * Domain of the external API provider (e.g., 'api.weather.com')
 * @member {String} provider_domain
 */
Connections.prototype['provider_domain'] = undefined;

/**
 * Optional.
 * @member {Boolean} verified_usage_ability
 */
Connections.prototype['verified_usage_ability'] = undefined;





/**
 * Allowed values for the <code>credential_type</code> property.
 * @enum {String}
 * @readonly
 */
Connections['CredentialTypeEnum'] = {

    /**
     * value: "SERVICE_ACCOUNT"
     * @const
     */
    "SERVICE_ACCOUNT": "SERVICE_ACCOUNT",

    /**
     * value: "USERNAME_PASSWORD"
     * @const
     */
    "USERNAME_PASSWORD": "USERNAME_PASSWORD",

    /**
     * value: "API_KEY"
     * @const
     */
    "API_KEY": "API_KEY",

    /**
     * value: "SERVICE_IDENTITY"
     * @const
     */
    "SERVICE_IDENTITY": "SERVICE_IDENTITY",

    /**
     * value: "ACCESS_KEY"
     * @const
     */
    "ACCESS_KEY": "ACCESS_KEY",

    /**
     * value: "NONE"
     * @const
     */
    "NONE": "NONE",

    /**
     * value: "OAUTH"
     * @const
     */
    "OAUTH": "OAUTH",

    /**
     * value: "JSON"
     * @const
     */
    "JSON": "JSON"
};



export default Connections;

