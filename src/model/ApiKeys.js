/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 1.24.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ApiKeys model module.
 * @module model/ApiKeys
 * @version 1.24.0
 */
class ApiKeys {
    /**
     * Constructs a new <code>ApiKeys</code>.
     * API Keys model storing every key created with SOC2 compliance
     * @alias module:model/ApiKeys
     * @param apiKeyId {Number} 
     * @param companyId {String} Company this API key belongs to
     * @param roleId {Number} Role/permission level for this API key
     */
    constructor(apiKeyId, companyId, roleId) { 
        
        ApiKeys.initialize(this, apiKeyId, companyId, roleId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, apiKeyId, companyId, roleId) { 
        obj['api_key_id'] = apiKeyId;
        obj['company_id'] = companyId;
        obj['role_id'] = roleId;
    }

    /**
     * Constructs a <code>ApiKeys</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ApiKeys} obj Optional instance to populate.
     * @return {module:model/ApiKeys} The populated <code>ApiKeys</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ApiKeys();

            if (data.hasOwnProperty('date_created')) {
                obj['date_created'] = ApiClient.convertToType(data['date_created'], 'Date');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('api_key_id')) {
                obj['api_key_id'] = ApiClient.convertToType(data['api_key_id'], 'Number');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('company_id')) {
                obj['company_id'] = ApiClient.convertToType(data['company_id'], 'String');
            }
            if (data.hasOwnProperty('role_id')) {
                obj['role_id'] = ApiClient.convertToType(data['role_id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('expiration_date_utc')) {
                obj['expiration_date_utc'] = ApiClient.convertToType(data['expiration_date_utc'], 'Date');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ApiKeys</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ApiKeys</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ApiKeys.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['company_id'] && !(typeof data['company_id'] === 'string' || data['company_id'] instanceof String)) {
            throw new Error("Expected the field `company_id` to be a primitive type in the JSON string but got " + data['company_id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

ApiKeys.RequiredProperties = ["api_key_id", "company_id", "role_id"];

/**
 * @member {Date} date_created
 */
ApiKeys.prototype['date_created'] = undefined;

/**
 * @member {Date} last_updated
 */
ApiKeys.prototype['last_updated'] = undefined;

/**
 * @member {Number} api_key_id
 */
ApiKeys.prototype['api_key_id'] = undefined;

/**
 * User who owns this API key
 * @member {String} user_id
 */
ApiKeys.prototype['user_id'] = undefined;

/**
 * Company this API key belongs to
 * @member {String} company_id
 */
ApiKeys.prototype['company_id'] = undefined;

/**
 * Role/permission level for this API key
 * @member {Number} role_id
 */
ApiKeys.prototype['role_id'] = undefined;

/**
 * Human-readable name for this API key
 * @member {String} name
 */
ApiKeys.prototype['name'] = undefined;

/**
 * When this API key expires (NULL = never expires)
 * @member {Date} expiration_date_utc
 */
ApiKeys.prototype['expiration_date_utc'] = undefined;






export default ApiKeys;

