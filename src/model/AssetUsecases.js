/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 1.25.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The AssetUsecases model module.
 * @module model/AssetUsecases
 * @version 1.25.0
 */
class AssetUsecases {
    /**
     * Constructs a new <code>AssetUsecases</code>.
     * Mapping of primary use cases for analytics assets, derived from clustering analysis
     * @alias module:model/AssetUsecases
     * @param aucName {String} Required. Must be unique.
     */
    constructor(aucName) { 
        
        AssetUsecases.initialize(this, aucName);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, aucName) { 
        obj['auc_name'] = aucName;
    }

    /**
     * Constructs a <code>AssetUsecases</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AssetUsecases} obj Optional instance to populate.
     * @return {module:model/AssetUsecases} The populated <code>AssetUsecases</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AssetUsecases();

            if (data.hasOwnProperty('date_created')) {
                obj['date_created'] = ApiClient.convertToType(data['date_created'], 'Date');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('auc_id')) {
                obj['auc_id'] = ApiClient.convertToType(data['auc_id'], 'Number');
            }
            if (data.hasOwnProperty('auc_name')) {
                obj['auc_name'] = ApiClient.convertToType(data['auc_name'], 'String');
            }
            if (data.hasOwnProperty('slug')) {
                obj['slug'] = ApiClient.convertToType(data['slug'], 'String');
            }
            if (data.hasOwnProperty('auc_description')) {
                obj['auc_description'] = ApiClient.convertToType(data['auc_description'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AssetUsecases</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AssetUsecases</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AssetUsecases.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['auc_name'] && !(typeof data['auc_name'] === 'string' || data['auc_name'] instanceof String)) {
            throw new Error("Expected the field `auc_name` to be a primitive type in the JSON string but got " + data['auc_name']);
        }
        // ensure the json data is a string
        if (data['slug'] && !(typeof data['slug'] === 'string' || data['slug'] instanceof String)) {
            throw new Error("Expected the field `slug` to be a primitive type in the JSON string but got " + data['slug']);
        }
        // ensure the json data is a string
        if (data['auc_description'] && !(typeof data['auc_description'] === 'string' || data['auc_description'] instanceof String)) {
            throw new Error("Expected the field `auc_description` to be a primitive type in the JSON string but got " + data['auc_description']);
        }

        return true;
    }


}

AssetUsecases.RequiredProperties = ["auc_name"];

/**
 * Optional.
 * @member {Date} date_created
 */
AssetUsecases.prototype['date_created'] = undefined;

/**
 * Optional.
 * @member {Date} last_updated
 */
AssetUsecases.prototype['last_updated'] = undefined;

/**
 * Unique identifier.
 * @member {Number} auc_id
 */
AssetUsecases.prototype['auc_id'] = undefined;

/**
 * Required. Must be unique.
 * @member {String} auc_name
 */
AssetUsecases.prototype['auc_name'] = undefined;

/**
 * URL-friendly slug derived from auc_name (e.g. 'competitive-benchmarking')
 * @member {String} slug
 */
AssetUsecases.prototype['slug'] = undefined;

/**
 * Optional.
 * @member {String} auc_description
 */
AssetUsecases.prototype['auc_description'] = undefined;






export default AssetUsecases;

