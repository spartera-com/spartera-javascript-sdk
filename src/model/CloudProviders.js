/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 1.24.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The CloudProviders model module.
 * @module model/CloudProviders
 * @version 1.24.0
 */
class CloudProviders {
    /**
     * Constructs a new <code>CloudProviders</code>.
     * Cloud providers (platforms) database
     * @alias module:model/CloudProviders
     * @param name {String} 
     */
    constructor(name) { 
        
        CloudProviders.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>CloudProviders</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CloudProviders} obj Optional instance to populate.
     * @return {module:model/CloudProviders} The populated <code>CloudProviders</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CloudProviders();

            if (data.hasOwnProperty('date_created')) {
                obj['date_created'] = ApiClient.convertToType(data['date_created'], 'Date');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'Date');
            }
            if (data.hasOwnProperty('provider_id')) {
                obj['provider_id'] = ApiClient.convertToType(data['provider_id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('parent_company')) {
                obj['parent_company'] = ApiClient.convertToType(data['parent_company'], 'String');
            }
            if (data.hasOwnProperty('marketing_homepage_url')) {
                obj['marketing_homepage_url'] = ApiClient.convertToType(data['marketing_homepage_url'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CloudProviders</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CloudProviders</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CloudProviders.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['parent_company'] && !(typeof data['parent_company'] === 'string' || data['parent_company'] instanceof String)) {
            throw new Error("Expected the field `parent_company` to be a primitive type in the JSON string but got " + data['parent_company']);
        }
        // ensure the json data is a string
        if (data['marketing_homepage_url'] && !(typeof data['marketing_homepage_url'] === 'string' || data['marketing_homepage_url'] instanceof String)) {
            throw new Error("Expected the field `marketing_homepage_url` to be a primitive type in the JSON string but got " + data['marketing_homepage_url']);
        }

        return true;
    }


}

CloudProviders.RequiredProperties = ["name"];

/**
 * @member {Date} date_created
 */
CloudProviders.prototype['date_created'] = undefined;

/**
 * @member {Date} last_updated
 */
CloudProviders.prototype['last_updated'] = undefined;

/**
 * @member {Number} provider_id
 */
CloudProviders.prototype['provider_id'] = undefined;

/**
 * @member {String} name
 */
CloudProviders.prototype['name'] = undefined;

/**
 * @member {String} parent_company
 */
CloudProviders.prototype['parent_company'] = undefined;

/**
 * @member {String} marketing_homepage_url
 */
CloudProviders.prototype['marketing_homepage_url'] = undefined;






export default CloudProviders;

