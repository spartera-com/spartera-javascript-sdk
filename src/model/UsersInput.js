/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 1.25.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The UsersInput model module.
 * @module model/UsersInput
 * @version 1.25.0
 */
class UsersInput {
    /**
     * Constructs a new <code>UsersInput</code>.
     * Input schema for creating User
     * @alias module:model/UsersInput
     * @param companyId {String} References companies.company_id — A Spartera seller or buyer company account. See GET /companies for valid values. Required.
     */
    constructor(companyId) { 
        
        UsersInput.initialize(this, companyId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, companyId) { 
        obj['company_id'] = companyId;
    }

    /**
     * Constructs a <code>UsersInput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UsersInput} obj Optional instance to populate.
     * @return {module:model/UsersInput} The populated <code>UsersInput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UsersInput();

            if (data.hasOwnProperty('company_id')) {
                obj['company_id'] = ApiClient.convertToType(data['company_id'], 'String');
            }
            if (data.hasOwnProperty('role_id')) {
                obj['role_id'] = ApiClient.convertToType(data['role_id'], 'Number');
            }
            if (data.hasOwnProperty('function_id')) {
                obj['function_id'] = ApiClient.convertToType(data['function_id'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
            if (data.hasOwnProperty('email_address')) {
                obj['email_address'] = ApiClient.convertToType(data['email_address'], 'String');
            }
            if (data.hasOwnProperty('timezone')) {
                obj['timezone'] = ApiClient.convertToType(data['timezone'], 'String');
            }
            if (data.hasOwnProperty('marketing_opt_out')) {
                obj['marketing_opt_out'] = ApiClient.convertToType(data['marketing_opt_out'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UsersInput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UsersInput</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UsersInput.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['company_id'] && !(typeof data['company_id'] === 'string' || data['company_id'] instanceof String)) {
            throw new Error("Expected the field `company_id` to be a primitive type in the JSON string but got " + data['company_id']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }
        // ensure the json data is a string
        if (data['email_address'] && !(typeof data['email_address'] === 'string' || data['email_address'] instanceof String)) {
            throw new Error("Expected the field `email_address` to be a primitive type in the JSON string but got " + data['email_address']);
        }
        // ensure the json data is a string
        if (data['timezone'] && !(typeof data['timezone'] === 'string' || data['timezone'] instanceof String)) {
            throw new Error("Expected the field `timezone` to be a primitive type in the JSON string but got " + data['timezone']);
        }

        return true;
    }


}

UsersInput.RequiredProperties = ["company_id"];

/**
 * References companies.company_id — A Spartera seller or buyer company account. See GET /companies for valid values. Required.
 * @member {String} company_id
 */
UsersInput.prototype['company_id'] = undefined;

/**
 * User's role for RBAC - single source of truth
 * @member {Number} role_id
 */
UsersInput.prototype['role_id'] = undefined;

/**
 * User's job function/title
 * @member {Number} function_id
 */
UsersInput.prototype['function_id'] = undefined;

/**
 * Required. One of: ACTIVE, PENDING, INACTIVE, BANNED.
 * @member {module:model/UsersInput.StatusEnum} status
 */
UsersInput.prototype['status'] = undefined;

/**
 * Optional. Must be unique.
 * @member {String} email_address
 */
UsersInput.prototype['email_address'] = undefined;

/**
 * Optional.
 * @member {String} timezone
 */
UsersInput.prototype['timezone'] = undefined;

/**
 * Whether user has opted out of marketing communications. Default false = opted in per ToS.
 * @member {Boolean} marketing_opt_out
 */
UsersInput.prototype['marketing_opt_out'] = undefined;





/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
UsersInput['StatusEnum'] = {

    /**
     * value: "ACTIVE"
     * @const
     */
    "ACTIVE": "ACTIVE",

    /**
     * value: "PENDING"
     * @const
     */
    "PENDING": "PENDING",

    /**
     * value: "INACTIVE"
     * @const
     */
    "INACTIVE": "INACTIVE",

    /**
     * value: "BANNED"
     * @const
     */
    "BANNED": "BANNED"
};



export default UsersInput;

