/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 0.0.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Cloudprovider model module.
 * @module model/Cloudprovider
 * @version 0.0.0
 */
class Cloudprovider {
    /**
     * Constructs a new <code>Cloudprovider</code>.
     * Cloud providers (platforms) database
     * @alias module:model/Cloudprovider
     * @param name {String} 
     */
    constructor(name) { 
        
        Cloudprovider.initialize(this, name);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name) { 
        obj['name'] = name;
    }

    /**
     * Constructs a <code>Cloudprovider</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Cloudprovider} obj Optional instance to populate.
     * @return {module:model/Cloudprovider} The populated <code>Cloudprovider</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Cloudprovider();

            if (data.hasOwnProperty('provider_id')) {
                obj['provider_id'] = ApiClient.convertToType(data['provider_id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('parent_company')) {
                obj['parent_company'] = ApiClient.convertToType(data['parent_company'], 'String');
            }
            if (data.hasOwnProperty('marketing_homepage_url')) {
                obj['marketing_homepage_url'] = ApiClient.convertToType(data['marketing_homepage_url'], 'String');
            }
            if (data.hasOwnProperty('date_created')) {
                obj['date_created'] = ApiClient.convertToType(data['date_created'], 'String');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'String');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Cloudprovider</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Cloudprovider</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Cloudprovider.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['provider_id'] && !(typeof data['provider_id'] === 'string' || data['provider_id'] instanceof String)) {
            throw new Error("Expected the field `provider_id` to be a primitive type in the JSON string but got " + data['provider_id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['parent_company'] && !(typeof data['parent_company'] === 'string' || data['parent_company'] instanceof String)) {
            throw new Error("Expected the field `parent_company` to be a primitive type in the JSON string but got " + data['parent_company']);
        }
        // ensure the json data is a string
        if (data['marketing_homepage_url'] && !(typeof data['marketing_homepage_url'] === 'string' || data['marketing_homepage_url'] instanceof String)) {
            throw new Error("Expected the field `marketing_homepage_url` to be a primitive type in the JSON string but got " + data['marketing_homepage_url']);
        }
        // ensure the json data is a string
        if (data['date_created'] && !(typeof data['date_created'] === 'string' || data['date_created'] instanceof String)) {
            throw new Error("Expected the field `date_created` to be a primitive type in the JSON string but got " + data['date_created']);
        }
        // ensure the json data is a string
        if (data['last_updated'] && !(typeof data['last_updated'] === 'string' || data['last_updated'] instanceof String)) {
            throw new Error("Expected the field `last_updated` to be a primitive type in the JSON string but got " + data['last_updated']);
        }
        // ensure the json data is a string
        if (data['active'] && !(typeof data['active'] === 'string' || data['active'] instanceof String)) {
            throw new Error("Expected the field `active` to be a primitive type in the JSON string but got " + data['active']);
        }

        return true;
    }


}

Cloudprovider.RequiredProperties = ["name"];

/**
 * @member {String} provider_id
 */
Cloudprovider.prototype['provider_id'] = undefined;

/**
 * @member {String} name
 */
Cloudprovider.prototype['name'] = undefined;

/**
 * @member {String} parent_company
 */
Cloudprovider.prototype['parent_company'] = undefined;

/**
 * @member {String} marketing_homepage_url
 */
Cloudprovider.prototype['marketing_homepage_url'] = undefined;

/**
 * @member {String} date_created
 */
Cloudprovider.prototype['date_created'] = undefined;

/**
 * @member {String} last_updated
 */
Cloudprovider.prototype['last_updated'] = undefined;

/**
 * @member {String} active
 */
Cloudprovider.prototype['active'] = undefined;






export default Cloudprovider;

