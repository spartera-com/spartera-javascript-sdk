/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 0.0.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The Alert model module.
 * @module model/Alert
 * @version 0.0.0
 */
class Alert {
    /**
     * Constructs a new <code>Alert</code>.
     * User alerts for when assets are updated
     * @alias module:model/Alert
     * @param assetId {String} 
     * @param companyId {String} 
     * @param alertType {String} 
     */
    constructor(assetId, companyId, alertType) { 
        
        Alert.initialize(this, assetId, companyId, alertType);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, assetId, companyId, alertType) { 
        obj['asset_id'] = assetId;
        obj['company_id'] = companyId;
        obj['alert_type'] = alertType;
    }

    /**
     * Constructs a <code>Alert</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/Alert} obj Optional instance to populate.
     * @return {module:model/Alert} The populated <code>Alert</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new Alert();

            if (data.hasOwnProperty('alert_id')) {
                obj['alert_id'] = ApiClient.convertToType(data['alert_id'], 'String');
            }
            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('company_id')) {
                obj['company_id'] = ApiClient.convertToType(data['company_id'], 'String');
            }
            if (data.hasOwnProperty('alert_type')) {
                obj['alert_type'] = ApiClient.convertToType(data['alert_type'], 'String');
            }
            if (data.hasOwnProperty('is_active')) {
                obj['is_active'] = ApiClient.convertToType(data['is_active'], 'String');
            }
            if (data.hasOwnProperty('last_triggered')) {
                obj['last_triggered'] = ApiClient.convertToType(data['last_triggered'], 'String');
            }
            if (data.hasOwnProperty('date_created')) {
                obj['date_created'] = ApiClient.convertToType(data['date_created'], 'String');
            }
            if (data.hasOwnProperty('last_updated')) {
                obj['last_updated'] = ApiClient.convertToType(data['last_updated'], 'String');
            }
            if (data.hasOwnProperty('active')) {
                obj['active'] = ApiClient.convertToType(data['active'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>Alert</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>Alert</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of Alert.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['alert_id'] && !(typeof data['alert_id'] === 'string' || data['alert_id'] instanceof String)) {
            throw new Error("Expected the field `alert_id` to be a primitive type in the JSON string but got " + data['alert_id']);
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['company_id'] && !(typeof data['company_id'] === 'string' || data['company_id'] instanceof String)) {
            throw new Error("Expected the field `company_id` to be a primitive type in the JSON string but got " + data['company_id']);
        }
        // ensure the json data is a string
        if (data['alert_type'] && !(typeof data['alert_type'] === 'string' || data['alert_type'] instanceof String)) {
            throw new Error("Expected the field `alert_type` to be a primitive type in the JSON string but got " + data['alert_type']);
        }
        // ensure the json data is a string
        if (data['is_active'] && !(typeof data['is_active'] === 'string' || data['is_active'] instanceof String)) {
            throw new Error("Expected the field `is_active` to be a primitive type in the JSON string but got " + data['is_active']);
        }
        // ensure the json data is a string
        if (data['last_triggered'] && !(typeof data['last_triggered'] === 'string' || data['last_triggered'] instanceof String)) {
            throw new Error("Expected the field `last_triggered` to be a primitive type in the JSON string but got " + data['last_triggered']);
        }
        // ensure the json data is a string
        if (data['date_created'] && !(typeof data['date_created'] === 'string' || data['date_created'] instanceof String)) {
            throw new Error("Expected the field `date_created` to be a primitive type in the JSON string but got " + data['date_created']);
        }
        // ensure the json data is a string
        if (data['last_updated'] && !(typeof data['last_updated'] === 'string' || data['last_updated'] instanceof String)) {
            throw new Error("Expected the field `last_updated` to be a primitive type in the JSON string but got " + data['last_updated']);
        }
        // ensure the json data is a string
        if (data['active'] && !(typeof data['active'] === 'string' || data['active'] instanceof String)) {
            throw new Error("Expected the field `active` to be a primitive type in the JSON string but got " + data['active']);
        }

        return true;
    }


}

Alert.RequiredProperties = ["asset_id", "company_id", "alert_type"];

/**
 * @member {String} alert_id
 */
Alert.prototype['alert_id'] = undefined;

/**
 * @member {String} asset_id
 */
Alert.prototype['asset_id'] = undefined;

/**
 * @member {String} user_id
 */
Alert.prototype['user_id'] = undefined;

/**
 * @member {String} company_id
 */
Alert.prototype['company_id'] = undefined;

/**
 * @member {String} alert_type
 */
Alert.prototype['alert_type'] = undefined;

/**
 * @member {String} is_active
 */
Alert.prototype['is_active'] = undefined;

/**
 * @member {String} last_triggered
 */
Alert.prototype['last_triggered'] = undefined;

/**
 * @member {String} date_created
 */
Alert.prototype['date_created'] = undefined;

/**
 * @member {String} last_updated
 */
Alert.prototype['last_updated'] = undefined;

/**
 * @member {String} active
 */
Alert.prototype['active'] = undefined;






export default Alert;

