/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 1.24.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The FavoritesInput model module.
 * @module model/FavoritesInput
 * @version 1.24.0
 */
class FavoritesInput {
    /**
     * Constructs a new <code>FavoritesInput</code>.
     * Input schema for creating Favorite
     * @alias module:model/FavoritesInput
     * @param assetId {String} 
     * @param companyId {String} 
     */
    constructor(assetId, companyId) { 
        
        FavoritesInput.initialize(this, assetId, companyId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, assetId, companyId) { 
        obj['asset_id'] = assetId;
        obj['company_id'] = companyId;
    }

    /**
     * Constructs a <code>FavoritesInput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/FavoritesInput} obj Optional instance to populate.
     * @return {module:model/FavoritesInput} The populated <code>FavoritesInput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new FavoritesInput();

            if (data.hasOwnProperty('asset_id')) {
                obj['asset_id'] = ApiClient.convertToType(data['asset_id'], 'String');
            }
            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('company_id')) {
                obj['company_id'] = ApiClient.convertToType(data['company_id'], 'String');
            }
            if (data.hasOwnProperty('notes')) {
                obj['notes'] = ApiClient.convertToType(data['notes'], 'String');
            }
            if (data.hasOwnProperty('category')) {
                obj['category'] = ApiClient.convertToType(data['category'], 'String');
            }
            if (data.hasOwnProperty('priority')) {
                obj['priority'] = ApiClient.convertToType(data['priority'], 'Number');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>FavoritesInput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>FavoritesInput</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of FavoritesInput.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['asset_id'] && !(typeof data['asset_id'] === 'string' || data['asset_id'] instanceof String)) {
            throw new Error("Expected the field `asset_id` to be a primitive type in the JSON string but got " + data['asset_id']);
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['company_id'] && !(typeof data['company_id'] === 'string' || data['company_id'] instanceof String)) {
            throw new Error("Expected the field `company_id` to be a primitive type in the JSON string but got " + data['company_id']);
        }
        // ensure the json data is a string
        if (data['notes'] && !(typeof data['notes'] === 'string' || data['notes'] instanceof String)) {
            throw new Error("Expected the field `notes` to be a primitive type in the JSON string but got " + data['notes']);
        }
        // ensure the json data is a string
        if (data['category'] && !(typeof data['category'] === 'string' || data['category'] instanceof String)) {
            throw new Error("Expected the field `category` to be a primitive type in the JSON string but got " + data['category']);
        }

        return true;
    }


}

FavoritesInput.RequiredProperties = ["asset_id", "company_id"];

/**
 * @member {String} asset_id
 */
FavoritesInput.prototype['asset_id'] = undefined;

/**
 * @member {String} user_id
 */
FavoritesInput.prototype['user_id'] = undefined;

/**
 * @member {String} company_id
 */
FavoritesInput.prototype['company_id'] = undefined;

/**
 * Optional user notes about this favorite
 * @member {String} notes
 */
FavoritesInput.prototype['notes'] = undefined;

/**
 * Optional category for organizing favorites (e.g., 'Work', 'Research')
 * @member {String} category
 */
FavoritesInput.prototype['category'] = undefined;

/**
 * User-defined priority for sorting (higher = more important)
 * @member {Number} priority
 */
FavoritesInput.prototype['priority'] = undefined;






export default FavoritesInput;

