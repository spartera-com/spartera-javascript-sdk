/**
 * Spartera API Documentation
 * Auto-generated API documentation for REST services of the Spartera platform
 *
 * The version of the OpenAPI document: 1.24.0
 * Contact: support@spartera.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ConnectionsInput model module.
 * @module model/ConnectionsInput
 * @version 1.24.0
 */
class ConnectionsInput {
    /**
     * Constructs a new <code>ConnectionsInput</code>.
     * Input schema for creating Connection
     * @alias module:model/ConnectionsInput
     * @param engineId {Number} 
     * @param companyId {String} 
     */
    constructor(engineId, companyId) { 
        
        ConnectionsInput.initialize(this, engineId, companyId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, engineId, companyId) { 
        obj['engine_id'] = engineId;
        obj['company_id'] = companyId;
    }

    /**
     * Constructs a <code>ConnectionsInput</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ConnectionsInput} obj Optional instance to populate.
     * @return {module:model/ConnectionsInput} The populated <code>ConnectionsInput</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ConnectionsInput();

            if (data.hasOwnProperty('user_id')) {
                obj['user_id'] = ApiClient.convertToType(data['user_id'], 'String');
            }
            if (data.hasOwnProperty('engine_id')) {
                obj['engine_id'] = ApiClient.convertToType(data['engine_id'], 'Number');
            }
            if (data.hasOwnProperty('company_id')) {
                obj['company_id'] = ApiClient.convertToType(data['company_id'], 'String');
            }
            if (data.hasOwnProperty('credential_type')) {
                obj['credential_type'] = ApiClient.convertToType(data['credential_type'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('description')) {
                obj['description'] = ApiClient.convertToType(data['description'], 'String');
            }
            if (data.hasOwnProperty('provider_domain')) {
                obj['provider_domain'] = ApiClient.convertToType(data['provider_domain'], 'String');
            }
            if (data.hasOwnProperty('verified_usage_ability')) {
                obj['verified_usage_ability'] = ApiClient.convertToType(data['verified_usage_ability'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ConnectionsInput</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ConnectionsInput</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ConnectionsInput.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['user_id'] && !(typeof data['user_id'] === 'string' || data['user_id'] instanceof String)) {
            throw new Error("Expected the field `user_id` to be a primitive type in the JSON string but got " + data['user_id']);
        }
        // ensure the json data is a string
        if (data['company_id'] && !(typeof data['company_id'] === 'string' || data['company_id'] instanceof String)) {
            throw new Error("Expected the field `company_id` to be a primitive type in the JSON string but got " + data['company_id']);
        }
        // ensure the json data is a string
        if (data['credential_type'] && !(typeof data['credential_type'] === 'string' || data['credential_type'] instanceof String)) {
            throw new Error("Expected the field `credential_type` to be a primitive type in the JSON string but got " + data['credential_type']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['description'] && !(typeof data['description'] === 'string' || data['description'] instanceof String)) {
            throw new Error("Expected the field `description` to be a primitive type in the JSON string but got " + data['description']);
        }
        // ensure the json data is a string
        if (data['provider_domain'] && !(typeof data['provider_domain'] === 'string' || data['provider_domain'] instanceof String)) {
            throw new Error("Expected the field `provider_domain` to be a primitive type in the JSON string but got " + data['provider_domain']);
        }

        return true;
    }


}

ConnectionsInput.RequiredProperties = ["engine_id", "company_id"];

/**
 * @member {String} user_id
 */
ConnectionsInput.prototype['user_id'] = undefined;

/**
 * @member {Number} engine_id
 */
ConnectionsInput.prototype['engine_id'] = undefined;

/**
 * @member {String} company_id
 */
ConnectionsInput.prototype['company_id'] = undefined;

/**
 * Enum type: CredentialType
 * @member {module:model/ConnectionsInput.CredentialTypeEnum} credential_type
 */
ConnectionsInput.prototype['credential_type'] = undefined;

/**
 * @member {String} name
 */
ConnectionsInput.prototype['name'] = undefined;

/**
 * @member {String} description
 */
ConnectionsInput.prototype['description'] = undefined;

/**
 * @member {String} provider_domain
 */
ConnectionsInput.prototype['provider_domain'] = undefined;

/**
 * @member {Boolean} verified_usage_ability
 */
ConnectionsInput.prototype['verified_usage_ability'] = undefined;





/**
 * Allowed values for the <code>credential_type</code> property.
 * @enum {String}
 * @readonly
 */
ConnectionsInput['CredentialTypeEnum'] = {

    /**
     * value: "SERVICE_ACCOUNT"
     * @const
     */
    "SERVICE_ACCOUNT": "SERVICE_ACCOUNT",

    /**
     * value: "USERNAME_PASSWORD"
     * @const
     */
    "USERNAME_PASSWORD": "USERNAME_PASSWORD",

    /**
     * value: "API_KEY"
     * @const
     */
    "API_KEY": "API_KEY",

    /**
     * value: "SERVICE_IDENTITY"
     * @const
     */
    "SERVICE_IDENTITY": "SERVICE_IDENTITY",

    /**
     * value: "ACCESS_KEY"
     * @const
     */
    "ACCESS_KEY": "ACCESS_KEY",

    /**
     * value: "NONE"
     * @const
     */
    "NONE": "NONE",

    /**
     * value: "OAUTH"
     * @const
     */
    "OAUTH": "OAUTH",

    /**
     * value: "JSON"
     * @const
     */
    "JSON": "JSON"
};



export default ConnectionsInput;

